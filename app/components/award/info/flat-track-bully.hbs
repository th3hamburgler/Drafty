<h1 class="text-2xl font-bold uppercase">Flat Track Bully</h1>

<p class="text-opacity-70">This Team doesn't care who they're up against, their
  team delivers the highest player points difference per game week.</p>

<table class="w-full">
  <thead>
    <tr class="border-b border-white border-opacity-20">
      <th>Team</th>
      <th class="text-right py-2 text-lg font-bold">Difference</th>
    </tr>
  </thead>
  <tbody>
    {{#each @totals as |row index|}}
      <tr
        class="border-b border-white border-opacity-20
          {{if (eq index 0) 'text-yellow'}}
          "
      >
        <td class="whitespace-nowrap py-2">
          <div class="text-xl font-bold">{{row.team}}</div>
          <div class="text-sm opacity-70">{{row.manager}}</div>
        </td>
        <td class="text-right py-2 text-2xl font-bold">{{pluralize
            row.total
            "pt"
          }}</td>
      </tr>
    {{/each}}
  </tbody>
</table>